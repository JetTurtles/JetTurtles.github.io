{
  "devDependencies": {
    "vitepress": "^1.6.3"
  },
  "scripts": {
    "blogs:dev": "vitepress dev blogs",
    "blogs:build": "vitepress build blogs",
    "blogs:preview": "vitepress preview blogs",
    "blogs:deploy": "npm run blogs:build && git checkout gh-pages && rm -rf ./docs/ && cp -r blogs/.vitepress/dist/* docs && git add . && git commit -m \"Deploy to GitHub Pages\" && git push origin gh-pages && git checkout -"
  }
}